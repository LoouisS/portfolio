---
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="refresh" content="0;url=/en/" />
    <title>Guille Sanchez — Full Stack Engineer</title>
    <script is:inline>
      const saved = localStorage.getItem("lang");
      const lang = saved === "es" || saved === "en" ? saved : navigator.language.startsWith("es") ? "es" : "en";
      location.replace(`/${lang}/`);
    </script>
  </head>
  <body></body>
</html>
